# 検索したいキーワードをカンマ区切りで指定
SEARCH_KEYWORDS="inflation, primordial black hole, axion"

# 検索対象のカテゴリ (例: quant-ph, cs.AI, hep-thなど。'all'で全カテゴリ)
SEARCH_CATEGORY="astro-ph.CO, astro-ph.GA, gr-qc, hep-th, hep-ph"

# --- メール送信設定 ---
# Gmail以外のサービスを使う場合は、プロバイダの指示に従ってください
SMTP_SERVER="smtp.gmail.com"
SMTP_PORT="587"
MAIL_SUBJECT="arXiv Alerter"

# --- 機密情報 ---
# 実運用では環境変数やGitHub Actions Secretsでの管理を推奨
SMTP_USER="your_email@example.com"
MAIL_FROM="your_email@example.com"
MAIL_TO="destination_email@example.com"
GEMINI_API_KEY="your_gemini_api_key"
SMTP_PASSWORD="your_smtp_password"

# --- テスト実行（任意） ---
# true の場合、メール送信をスキップして本文を標準出力に表示
TEST_MODE="false"
